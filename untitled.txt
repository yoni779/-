<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>מתוק בכיכר – קיוסק חלבי</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="מתוק בכיכר – קיוסק חלבי עם טוסטים, פסטות, וופל בלגי, סלטים ועוד. טע fresh, שירות חם ואווירה טובה." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Assistant:wght@300;400;600;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --bg: #f7f7fb;
      --primary: #ff7b3a;
      --primary-dark: #e15f20;
      --accent: #00bfa6;
      --text: #222;
      --muted: #777;
      --card-bg: #ffffff;
      --shadow-soft: 0 10px 25px rgba(15, 23, 42, 0.08);
      --radius-lg: 18px;
      --radius-xl: 28px;
      --transition-fast: 0.2s ease-out;
    }

    * {
      box-sizing: border-box;
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: "Assistant", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      background: radial-gradient(circle at top left, #ffeede, #f7f7fb 40%, #f7f7fb 100%);
      color: var(--text);
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    img {
      max-width: 100%;
      display: block;
    }

    /* Layout */
    .container {
      width: min(1100px, 92vw);
      margin: 0 auto;
    }

    header {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(14px);
      background: rgba(247, 247, 251, 0.85);
      border-bottom: 1px solid rgba(148, 163, 184, 0.25);
    }

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.7rem 0;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-weight: 700;
      font-size: 1.2rem;
    }

    .logo-icon {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background: radial-gradient(circle at 30% 20%, #ffe1c7, #ff7b3a);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-weight: 800;
      font-size: 1.1rem;
      box-shadow: var(--shadow-soft);
    }

    .nav-links {
      display: flex;
      gap: 1.2rem;
      font-size: 0.95rem;
    }

    .nav-links a {
      position: relative;
      padding-bottom: 0.2rem;
    }

    .nav-links a::after {
      content: "";
      position: absolute;
      right: 0;
      bottom: 0;
      width: 0;
      height: 2px;
      border-radius: 999px;
      background: linear-gradient(90deg, var(--primary), var(--accent));
      transition: width var(--transition-fast);
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    .nav-cta {
      display: none;
    }

    /* Buttons */
    .btn {
      border: none;
      border-radius: 999px;
      padding: 0.6rem 1.4rem;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.35rem;
      transition: transform var(--transition-fast), box-shadow var(--transition-fast), background var(--transition-fast);
      white-space: nowrap;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary), var(--primary-dark));
      color: #fff;
      box-shadow: 0 10px 20px rgba(248, 113, 113, 0.35);
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 16px 32px rgba(248, 113, 113, 0.4);
      background: linear-gradient(135deg, var(--primary-dark), var(--primary));
    }

    .btn-outline {
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(148, 163, 184, 0.5);
      color: var(--text);
    }

    .btn-outline:hover {
      background: #fff;
      box-shadow: var(--shadow-soft);
      transform: translateY(-1px);
    }

    /* Hero */
    .hero {
      padding: 2.4rem 0 2.8rem;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
      gap: 2.5rem;
      align-items: center;
    }

    .hero-eyebrow {
      font-size: 0.9rem;
      color: var(--accent);
      font-weight: 600;
      letter-spacing: 0.03em;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      background: rgba(0, 191, 166, 0.08);
      padding: 0.2rem 0.8rem;
      border-radius: 999px;
    }

    .hero-title {
      font-size: clamp(2.1rem, 4vw, 2.7rem);
      margin: 0.7rem 0 0.4rem;
      line-height: 1.2;
    }

    .hero-title span {
      color: var(--primary-dark);
    }

    .hero-subtitle {
      font-size: 1rem;
      color: var(--muted);
      max-width: 30rem;
      line-height: 1.55;
      margin-bottom: 1.2rem;
    }

    .hero-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 0.6rem;
      margin-bottom: 1.3rem;
    }

    .badge {
      font-size: 0.78rem;
      padding: 0.25rem 0.7rem;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(226, 232, 240, 0.9);
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      color: var(--muted);
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.7rem;
      align-items: center;
      margin-bottom: 1rem;
    }

    .hero-hours {
      font-size: 0.86rem;
      color: var(--muted);
    }

    .hero-tagline {
      font-size: 0.9rem;
      color: var(--accent);
      font-weight: 600;
      margin-top: 0.3rem;
    }

    .hero-visual {
      position: relative;
    }

    .hero-card {
      background: rgba(255, 255, 255, 0.96);
      border-radius: var(--radius-xl);
      padding: 1.1rem 1rem;
      box-shadow: var(--shadow-soft);
      position: relative;
      overflow: hidden;
    }

    .hero-card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.7rem;
    }

    .hero-card-title {
      font-weight: 700;
      font-size: 1rem;
    }

    .hero-chip {
      font-size: 0.78rem;
      padding: 0.25rem 0.7rem;
      border-radius: 999px;
      background: rgba(248, 250, 252, 0.95);
      border: 1px solid rgba(148, 163, 184, 0.45);
      color: var(--muted);
    }

    .hero-food-grid {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: 0.8rem;
      align-items: stretch;
    }

    .food-main-card {
      border-radius: 20px;
      background: linear-gradient(135deg, #fff7ec, #ffe0cc);
      padding: 0.8rem;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .food-main-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 0.4rem;
    }

    .food-main-text h3 {
      margin: 0;
      font-size: 0.98rem;
    }

    .food-main-text p {
      margin: 0.15rem 0 0.35rem;
      font-size: 0.8rem;
      color: var(--muted);
    }

    .food-main-price {
      font-weight: 700;
      font-size: 1.05rem;
      color: var(--primary-dark);
    }

    .food-main-tag {
      font-size: 0.72rem;
      padding: 0.2rem 0.55rem;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.95);
      border: 1px solid rgba(254, 215, 170, 0.9);
      color: var(--muted);
    }

    .food-main-img {
      width: 80px;
      height: 80px;
      border-radius: 18px;
      background: radial-gradient(circle at 30% 20%, #fffaf5, #ffba7f);
      box-shadow: 0 12px 25px rgba(248, 113, 113, 0.3);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
    }

    .food-secondary {
      display: grid;
      grid-template-rows: repeat(2, minmax(0, 1fr));
      gap: 0.6rem;
    }

    .small-food-card {
      border-radius: 16px;
      padding: 0.5rem 0.7rem;
      background: #fff;
      box-shadow: 0 6px 16px rgba(15, 23, 42, 0.08);
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.8rem;
    }

    .small-food-title {
      font-weight: 600;
      margin-bottom: 0.15rem;
    }

    .small-food-meta {
      font-size: 0.72rem;
      color: var(--muted);
    }

    .small-food-price {
      font-weight: 700;
      font-size: 0.9rem;
    }

    .small-dot {
      width: 9px;
      height: 9px;
      border-radius: 50%;
      background: var(--accent);
    }

    .hero-floating {
      position: absolute;
      left: -12px;
      bottom: -18px;
      background: rgba(15, 23, 42, 0.94);
      color: #f9fafb;
      border-radius: 999px;
      padding: 0.55rem 0.9rem;
      font-size: 0.76rem;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      box-shadow: 0 16px 30px rgba(15, 23, 42, 0.7);
    }

    .hero-floating-dot {
      width: 9px;
      height: 9px;
      border-radius: 50%;
      background: #22c55e;
      box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.25);
    }

    /* Sections */
    section {
      padding: 2.2rem 0;
    }

    .section-title {
      font-size: 1.6rem;
      margin-bottom: 0.3rem;
      text-align: center;
    }

    .section-subtitle {
      font-size: 0.96rem;
      color: var(--muted);
      text-align: center;
      margin-bottom: 1.6rem;
    }

    /* Menu Section */
    .menu-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1.1rem;
    }

    .menu-card {
      background: var(--card-bg);
      border-radius: var(--radius-lg);
      padding: 1rem;
      box-shadow: var(--shadow-soft);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 0.6rem;
    }

    .menu-card-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 0.6rem;
    }

    .menu-name {
      font-weight: 700;
      font-size: 1rem;
    }

    .menu-desc {
      font-size: 0.86rem;
      color: var(--muted);
      margin-top: 0.15rem;
    }

    .menu-price {
      font-weight: 700;
      color: var(--primary-dark);
      font-size: 1.02rem;
    }

    .menu-tag {
      font-size: 0.78rem;
      padding: 0.18rem 0.55rem;
      border-radius: 999px;
      background: rgba(0, 191, 166, 0.08);
      color: var(--accent);
      border: 1px solid rgba(0, 191, 166, 0.25);
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
    }

    .menu-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.78rem;
      color: var(--muted);
    }

    /* Info / Hours */
    .info-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.1fr);
      gap: 1.5rem;
      align-items: flex-start;
    }

    .info-card {
      background: var(--card-bg);
      border-radius: var(--radius-lg);
      padding: 1rem 1.1rem;
      box-shadow: var(--shadow-soft);
    }

    .info-card h3 {
      margin: 0 0 0.6rem;
      font-size: 1.1rem;
    }

    .info-row {
      display: flex;
      justify-content: space-between;
      font-size: 0.9rem;
      padding: 0.25rem 0;
      border-bottom: 1px dashed rgba(148, 163, 184, 0.3);
    }

    .info-row:last-child {
      border-bottom: none;
    }

    .info-label {
      color: var(--muted);
    }

    .info-value {
      font-weight: 600;
    }

    .map-placeholder {
      margin-top: 0.7rem;
      border-radius: 16px;
      overflow: hidden;
      height: 210px;
      background: #e2e8f0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.9rem;
      color: var(--muted);
    }

    iframe {
      border: 0;
      width: 100%;
      height: 100%;
      display: block;
    }

    /* Gallery / vibe */
    .gallery {
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: 1.2rem;
    }

    .gallery-main {
      border-radius: var(--radius-lg);
      background: linear-gradient(135deg, #fef3c7, #fecaca);
      padding: 1rem 1rem 0.4rem;
      box-shadow: var(--shadow-soft);
      display: flex;
      flex-direction: column;
      gap: 0.6rem;
    }

    .gallery-chips {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
      font-size: 0.78rem;
    }

    .chip {
      padding: 0.2rem 0.7rem;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(248, 250, 252, 0.9);
    }

    .gallery-text {
      font-size: 0.92rem;
      color: #374151;
      margin-bottom: 0.4rem;
    }

    .gallery-side {
      display: grid;
      grid-template-rows: repeat(2, minmax(0, 1fr));
      gap: 0.8rem;
    }

    .mini-card {
      background: var(--card-bg);
      border-radius: 16px;
      padding: 0.8rem;
      box-shadow: var(--shadow-soft);
      font-size: 0.88rem;
      display: flex;
      flex-direction: column;
      gap: 0.35rem;
    }

    .mini-title {
      font-weight: 600;
    }

    .mini-meta {
      font-size: 0.8rem;
      color: var(--muted);
    }

    /* Contact */
    .contact-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: 1.6rem;
      align-items: center;
    }

    .contact-card {
      background: var(--card-bg);
      border-radius: var(--radius-xl);
      padding: 1.3rem 1.4rem;
      box-shadow: var(--shadow-soft);
    }

    .contact-card h3 {
      margin-top: 0;
      margin-bottom: 0.4rem;
    }

    .contact-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 0.7rem;
      margin-top: 0.45rem;
      font-size: 0.9rem;
    }

    .contact-label {
      color: var(--muted);
    }

    .contact-value {
      font-weight: 600;
    }

    .contact-note {
      font-size: 0.84rem;
      color: var(--muted);
      margin-top: 0.7rem;
    }

    .contact-form {
      display: grid;
      gap: 0.5rem;
      margin-top: 0.8rem;
      font-size: 0.86rem;
    }

    .field-group {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .field-group label {
      font-size: 0.82rem;
      color: var(--muted);
    }

    input[type="text"],
    input[type="tel"],
    textarea {
      font-family: inherit;
      font-size: 0.9rem;
      padding: 0.5rem 0.7rem;
      border-radius: 10px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      outline: none;
      transition: border-color var(--transition-fast), box-shadow var(--transition-fast), background var(--transition-fast);
      background: rgba(255, 255, 255, 0.96);
    }

    textarea {
      resize: vertical;
      min-height: 82px;
    }

    input:focus,
    textarea:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 1px rgba(0, 191, 166, 0.4);
      background: #fff;
    }

    .contact-hero-text {
      font-size: 0.9rem;
      color: var(--muted);
      margin-bottom: 0.7rem;
    }

    /* Footer */
    footer {
      padding: 1.4rem 0 1.8rem;
      font-size: 0.8rem;
      color: var(--muted);
      border-top: 1px solid rgba(148, 163, 184, 0.2);
      margin-top: 1rem;
    }

    .footer-row {
      display: flex;
      justify-content: space-between;
      gap: 0.8rem;
      flex-wrap: wrap;
      align-items: center;
    }

    /* Floating WhatsApp */
    .whatsapp-float {
      position: fixed;
      left: 1.1rem;
      bottom: 1.1rem;
      z-index: 40;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: #25d366;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 12px 24px rgba(34, 197, 94, 0.4);
      cursor: pointer;
      transition: transform var(--transition-fast), box-shadow var(--transition-fast);
    }

    .whatsapp-float:hover {
      transform: translateY(-2px);
      box-shadow: 0 18px 32px rgba(34, 197, 94, 0.55);
    }

    .whatsapp-float span {
      font-size: 1.6rem;
      color: #fff;
    }

    /* Responsive */
    @media (max-width: 900px) {
      .hero-grid,
      .menu-grid,
      .info-grid,
      .gallery,
      .contact-grid {
        grid-template-columns: 1fr;
      }

      .hero {
        padding-top: 1.6rem;
      }

      .hero-visual {
        order: -1;
      }

      .nav-links {
        display: none;
      }

      .nav-cta {
        display: block;
      }
    }

    @media (max-width: 640px) {
      .hero {
        padding-bottom: 2rem;
      }

      .hero-card {
        border-radius: 20px;
      }

      .menu-grid {
        grid-template-columns: 1fr;
      }

      .gallery {
        gap: 0.9rem;
      }

      .section-title {
        font-size: 1.45rem;
      }

      .hero-title {
        font-size: 1.9rem;
      }

      .hero-subtitle {
        font-size: 0.92rem;
      }

      .container {
        width: 92vw;
      }
    }
  </style>
</head>
<body>
  <!-- Floating WhatsApp – החלף למספר שלך -->
  <a
    class="whatsapp-float"
    href="https://wa.me/972501234567?text=%D7%94%D7%99%D7%99%20%D7%A8%D7%A6%D7%99%D7%AA%D7%99%20%D7%9C%D7%94%D7%96%D7%9E%D7%99%D7%9F%20%D7%9E%D7%AA%D7%95%D7%A7%20%D7%91%D7%9B%D7%99%D7%9B%D7%A8"
    target="_blank"
    aria-label="הזמנה בוואטסאפ"
  >
    <span>⌕</span>
  </a>

  <header>
    <div class="container nav">
      <div class="logo">
        <div class="logo-icon">🍓</div>
        <div>
          <div>מתוק בכיכר</div>
          <div style="font-size: 0.78rem; color: var(--muted);">קיוסק חלבי • טעים ברמה אחרת</div>
        </div>
      </div>
      <nav class="nav-links">
        <a href="#menu">תפריט</a>
        <a href="#hours">שעות וכתובת</a>
        <a href="#gallery">אווירה</a>
        <a href="#contact">יצירת קשר</a>
      </nav>
      <div class="nav-cta">
        <a
          href="https://wa.me/972501234567?text=%D7%94%D7%99%D7%99%20%D7%A8%D7%A6%D7%99%D7%AA%D7%99%20%D7%9C%D7%94%D7%96%D7%9E%D7%99%D7%9F"
          target="_blank"
          class="btn btn-primary"
        >
          הזמנה בוואטסאפ
        </a>
      </div>
    </div>
  </header>

  <main>
    <!-- Hero -->
    <section class="hero" id="top">
      <div class="container hero-grid">
        <div>
          <div class="hero-eyebrow">
            ✨ טרי, חם ומוכן בשבילך
          </div>
          <h1 class="hero-title">
            מתוק בכיכר – הקיוסק החלבי
            <span>שלא מפסיקים לחזור אליו</span>
          </h1>
          <p class="hero-subtitle">
            טוסטים, פסטות מוקרמות, וופל בלגי, סלט טרי, מלאווח פיצה ועוד. אוכל מפנק,
            שירות חם ואווירה של בית.
          </p>

          <div class="hero-badges">
            <div class="badge">🧀 חלבי בלבד</div>
            <div class="badge">🥪 הכל מוכן במקום</div>
            <div class="badge">🚶‍♂️ מושלם על הדרך</div>
          </div>

          <div class="hero-actions">
            <a
              class="btn btn-primary"
              href="https://wa.me/972501234567?text=%D7%94%D7%99%D7%99%20%D7%A8%D7%A6%D7%99%D7%AA%D7%99%20%D7%9C%D7%94%D7%96%D7%9E%D7%99%D7%9F%20%D7%94%D7%96%D7%9E%D7%A0%D7%94%20%D7%98%D7%9C%D7%A4%D7%95%D7%9F"
              target="_blank"
            >
              הזמנה מהירה בוואטסאפ
            </a>
            <a class="btn btn-outline" href="#menu">להצגת התפריט</a>
          </div>

          <div class="hero-hours">
            ⏰ היום פתוח: 10:00–23:30 (ניתן להתאים לפי המציאות)
            <div class="hero-tagline">
              מגיע רעב? אנחנו כבר נדאג לשאר.
            </div>
          </div>
        </div>

        <div class="hero-visual">
          <div class="hero-card">
            <div class="hero-card-header">
              <div class="hero-card-title">הכי נמכר היום</div>
              <div class="hero-chip">טרי עכשיו על הפלאנצ'ה</div>
            </div>

            <div class="hero-food-grid">
              <div class="food-main-card">
                <div class="food-main-top">
                  <div class="food-main-text">
                    <h3>טוסט מיוחד מתוק בכיכר</h3>
                    <p>לחם ט